_target_: src.nn.data.connectfour_datamodule.ConnectFourDataModule
_recursive_: false

batch_size: ${timekeeping.batch_size}
num_workers: ${timekeeping.num_workers}

# Dataset configuration
num_train_samples: 100000  # Random positions per epoch (bootstrap)
num_val_samples: 1000

# Board configuration
vocab_size: 4  # empty(1), player1(2), player2(3), padding(0)
board_rows: 6
board_cols: 7

# Memory settings
pin_memory: true