_target_: src.nn.data.ARCDataModule
_recursive_: false
data_dir: "data"
batch_size: ${timekeeping.batch_size}
num_workers: ${timekeeping.num_workers}
max_grid_size: 30
augment_train: false
samples_per_task: 100  # For augmentation
