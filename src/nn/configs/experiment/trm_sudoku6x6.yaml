# @package _global_

defaults:
  - override /data: sudoku6x6
  - override /model: trm
  - override /callbacks: default
  - override /trainer: gpu

tags: ["arc", "trm"]

timekeeping:
  max_epochs: 750
  batch_size: 768
  num_workers: 0

model_tuning:
  learning_rate: 1e-3
  learning_rate_emb: 1e-2
  warmup_steps: 2000
  weight_decay: 0.1
  lr_min_ratio: 1.0
  num_heads: 2
  halt_exploration_prob: 0.1
  N_supervision: 6
  N_supervision_val: 6
  H_cycles: 2
  L_cycles: 2
  num_layers: 1
  max_steps: 0
  puzzle_emb_dim: 4
  puzzle_emb_len: 4
  hidden_size: 64
  ffn_expansion: 2
  rope_theta: 10000

trainer:
  check_val_every_n_epoch: 10

save_dir: /tmp/ml-experiments
append_wandb_name_to_save_dir: true