# @package _global_

defaults:
  - override /data: sudoku6x6_hybrid
  - override /model: trm
  - override /callbacks: default
  - override /trainer: gpu

tags: ["sudoku", "trm"]

timekeeping:
  max_epochs: 750
  batch_size: 768
  num_workers: 0

model_tuning:
  learning_rate: 1e-3
  learning_rate_emb: 1e-2
  warmup_steps: 2000
  weight_decay: 0.1
  lr_min_ratio: 1.0
  num_heads: 8
  halt_exploration_prob: 0.1
  N_supervision: 6
  N_supervision_val: 16
  H_cycles: 3
  L_cycles: 1
  num_layers: 1
  max_steps: 0
  puzzle_emb_dim: 0
  puzzle_emb_len: 0
  hidden_size: 256
  ffn_expansion: 2
  rope_theta: 10000

trainer:
  check_val_every_n_epoch: 1

save_dir: /tmp/ml-experiments
append_wandb_name_to_save_dir: true