_target_: src.nn.models.fairness_detection_module.TRMModule
_recursive_: false  # Prevent recursive instantiation of nested configs

hidden_size: 512
num_layers: 2
max_grid_size: 30
num_colors: 10
n_latent_recursions: 6
T_deep_recursions: 3
N_supervision: 16
learning_rate: 1e-4
weight_decay: 0.01
warmup_steps: 2000
max_steps: 100000
ema_decay: 0.999
use_ema: True